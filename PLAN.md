# Pulse OS Plan (v1)

## What exists
- Shared SQLite schema (legacy + X Agent OS tables).
- Python agent-service with daily pipeline + daily brief generator.
- Next.js dashboard with `/today` and `/skills` flows.

## Next focus
1. Validate daily pipeline output in SQLite.
2. Add metrics provider integration.
3. Expand Sessions and Explorer pages.
4. Build Analytics charts.
